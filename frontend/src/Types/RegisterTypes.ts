export type RegisterForm = {
    name: string;
    email: string;
    password: string;
    password2:string;
}

export type LoginForm = {
    email: string;
    password: string;
}

export type State = {
    
}
